export enum EMAIL_ERROR {
    NOT_EXISTS = 'Email does not exists!',
    INVALID_ROLE = `Unauthorized account role!`,
    EMAIL = 'Please provide a valid email!',
    REQUIRED = 'Email is required!'
}